<div class="row">
  <div class="col-md-8">
    <%= attachment_image_tag @post, :post_image %>
    <p>タイトル:<%= @post.title %></p>
    <p>説明:<%= @post.text %></p>
    <p>投稿日時:<%= @post.created_at.strftime('%Y/%m/%d') %></p>
  </div>
</div>