<!-- コメント入力フォーム ------------------------------------------------------------>
<%= form_with(model: [post, newpost_comment], url: post_post_comments_path(post) ) do |f| %>
    <%= f.text_area :comment, class: "input-mysize" %>
    <%= f.submit "送信", class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>